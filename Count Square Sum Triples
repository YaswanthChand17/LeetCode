class Solution(object):
    def countTriples(self, n):
        count=0
        for a in range(1,n+1):
            for b in range(1,n+1):
                sq= a*a+b*b
                c=int(sq**0.5)
                if c<=n and c*c==sq:
                    count+=1
        return count
